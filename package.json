{
  "name": "esrol-initializer",
  "description": "An Initializer Class used for adding and initializing components. Priority must exist between them.",
  "version": "0.1.0",
  "main": "index.js",
  "author": "Ivaylo Ivanov <ivaylo.ivanov.ipi@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git://github.com/esrol/esrol-initializer"
  },
  "bugs": {
    "url": "https://github.com/esrol/esrol-initializer"
  },
  "license": "MIT",
  "keywords": [
    "esrol",
    "initializer",
    "intialize",
    "component",
    "components"
  ],
  "engines": {
    "node": ">= 4.0.0"
  },
  "config": {},
  "scripts": {
    "test": "node_modules/.bin/mocha ./tests/test",
    "coverage": "istanbul cover node_modules/mocha/bin/_mocha ./tests/test",
    "coveralls": "cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "devDependencies": {
    "chai": "^3.4.0",
    "mocha": "^2.3.3",
    "sinon": "^1.17.2",
    "mocha-sinon": "^1.1.4",
    "debug": "^2.2.0",
    "coveralls": "^2.11.4",
    "istanbul": "^0.4.1"
  },
  "dependencies": {
    "esrol-errors": "^0.3.4"
  }
}
